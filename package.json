{
	"name": "cozy-cv",
	"type": "module",
	"engines": {
		"node": "22.x"
	},
	"scripts": {
		"build": "vite build",
		"dev": "vite",
		"preview": "vite preview",
		"zip": "npm run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\""
	},
	"dependencies": {
		"kaplay": "^3001.0.19",
		"posthog-js": "^1.393.0"
	},
	"devDependencies": {
		"@types/node": "^26.0.0",
		"vite": "^8.0.16"
	}
}
